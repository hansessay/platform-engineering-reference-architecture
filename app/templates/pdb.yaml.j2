apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ service_name }}
  namespace: {{ environment }}-{{ owner }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: {{ service_name }}
