apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ service_name }}-default-deny
  namespace: {{ environment }}-{{ owner }}
spec:
  podSelector:
    matchLabels:
      app: {{ service_name }}
  policyTypes:
    - Ingress
    - Egress